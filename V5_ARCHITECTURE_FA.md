# V5 — سیستم مدیریت سرمایه Hyperliquid-only

V5 لایه تصمیم‌گیری و مدیریت سرمایه را بالای استراتژی‌های موجود قرار می‌دهد.

## اصل اصلی
استراتژی فقط «فرصت» پیشنهاد می‌دهد. هیچ استراتژی به‌تنهایی اجازه ورود ندارد. مغز مرکزی فرصت‌ها را رتبه‌بندی می‌کند، وضعیت بازار را می‌سنجد، ریسک کل سبد را محاسبه می‌کند و سپس اجازه یا رد ورود را صادر می‌کند.

## اجزای اولیه
- `v5/regime.py`: تشخیص احتمالی وضعیت بازار؛ روند صعودی/نزولی، خنثی و شوک.
- `v5/opportunity.py`: قالب استاندارد فرصت مستقل از استراتژی.
- `v5/risk.py`: سقف ریسک معامله، ریسک کل سبد، ریسک هم‌جهت، زیان روزانه و افت سرمایه.
- `v5/brain.py`: تصمیم نهایی و توضیح قابل ثبت برای هر فرصت.
- `v5/journal.py`: دفتر تصمیمات.
- `v5/portfolio_sim.py`: حساب مشترک برای تحقیق؛ جلوی خطای «چند حساب مستقل که بعداً ترکیب شده‌اند» را می‌گیرد.

## قوانین ایمنی اولیه
1. سقف ریسک هر معامله 1.25% است و هیچ ضریب حجم نباید این سقف را دور بزند.
2. سقف گرمای ریسک سبد 2.5% است.
3. سقف ریسک معاملات هم‌جهت 1.8% است.
4. بعد از افت سرمایه 8% سیستم وارد حالت دفاعی می‌شود.
5. بعد از افت 15% ورود جدید متوقف می‌شود.
6. زیان روزانه 2% ورود جدید را متوقف می‌کند.
7. افزایش حجم فقط از طریق لایه ریسک مجاز است؛ استراتژی نمی‌تواند مستقیماً حجم را تحمیل کند.

این اعداد «نسخه تحقیقاتی اولیه» هستند و قبل از پول واقعی باید با آزمایش حساسیت و آزمون خارج از نمونه اعتبارسنجی شوند.
