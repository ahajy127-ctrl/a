اگر پنجره بک‌تست بسته نمی‌شد:
1) روی START_BACKTEST.bat دوبار کلیک کن.
2) این نسخه در صورت خطا پنجره را باز نگه می‌دارد.
3) متن خطا را از پنجره برای من بفرست، یا فایل backtest_run.log را بفرست.
4) اگر Python نصب نباشد، خود برنامه پیام واضح می‌دهد.

برای اجرای بک‌تست فعلاً هیچ تنظیمی را تغییر نده.
