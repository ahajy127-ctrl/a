# گزارش ممیزی V1.6

## تغییرات اصلی
- ساعت بازار با منطقه زمانی واقعی لندن، نیویورک و توکیو و پشتیبانی خودکار از تغییر ساعت تابستانی.
- امتیاز کیفیت ۰ تا ۱۰۰ بر اساس گروه‌های مستقل شواهد، برای جلوگیری از شمارش چندباره اندیکاتورهای هم‌خانواده.
- ریسک تطبیقی بر اساس کیفیت معامله، با سقف سخت ۱.۲۵٪ از سرمایه در هر معامله.
- کاهش ریسک در ساعات کم‌حجم و شرایط طوفانی.
- جداسازی کامل «ریسک حساب»، «مارجین» و «اهرم». مارجین از ریسک دلاری و فاصله Stop محاسبه می‌شود.
- بک‌تست جدید با OHLC واقعی، اجرای درون‌ساعتی، کارمزد، لغزش، Funding اختیاری، جلسه بازار و ریسک تطبیقی.
- Walk-Forward متحرک ۹۰ روز آموزش / ۳۰ روز آزمایش خارج از نمونه.
- Monte Carlo روی ترتیب معاملات.
- ابزار `backtest_lab.py` برای دریافت داده عمومی Hyperliquid و ساخت CSV قابل تکرار.
- `requirements.txt` اضافه شد و Preflight اکنون بدون خطا پاس می‌شود.

## تست‌های انجام‌شده
- Python compile: PASS
- Preflight: PASS — 0 failure
- Risk Guard tests: PASS
- Self-test: 25/25 PASS
- تست مستقل ساعت بازار و تغییر ساعت تابستانی: PASS
- تست آفلاین بک‌تست روی داده ساختگی OHLC: PASS؛ موتور توانست ورود، Stop/TP درون کندل، کارمزد، لغزش و ریسک تطبیقی را اجرا کند.

## محدودیت مهم
بک‌تست تاریخی واقعی Hyperliquid در محیط اجرای فعلی اجرا نشد، چون این محیط دسترسی خروجی شبکه برای درخواست‌های POST به API صرافی ندارد. برای جلوگیری از ساختن عدد جعلی، هیچ نتیجه سود/ضرر تاریخی ساختگی به‌عنوان نتیجه واقعی گزارش نشده است.

با اجرای:

```bash
python backtest_lab.py --coins BTC,ETH,SOL --days 180 --walk-forward
```

داده واقعی از API عمومی Hyperliquid دریافت و در `research_data/` ذخیره می‌شود و گزارش نهایی ساخته می‌شود.
