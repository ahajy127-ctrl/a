# اتصال V5 به هسته مرکزی

این نسخه، V5 Capital OS را به هسته استراتژی V4 متصل می‌کند.

## مسیر تصمیم

استراتژی‌های قدیمی V4 فقط «پیشنهاد معامله» می‌دهند:

- S1 = شکست دونچیان + ADX
- S2 = تقاطع EMA و فقط خرید

سپس V5 این پیشنهاد را بررسی می‌کند:

1. وضعیت بازار
2. کیفیت فرصت
3. تناسب استراتژی با وضعیت بازار
4. ریسک معاملات باز
5. سقف ریسک هر دارایی
6. سقف ریسک کل سبد
7. سقف ریسک هم‌جهت
8. افت سرمایه و محدودیت‌های دفاعی

فقط بعد از تأیید V5، همان `live_engine` قبلی معامله را باز/مدیریت می‌کند.

## نکته مهم

هیچ سفارش واقعی به صرافی اضافه نشده است. اتصال فعلی برای **تحقیق و Paper** است.

## اجرای تست

```bash
python -m v5.tests
```

## Paper

در `v2/paper.py` متغیر `V5_BRAIN_ENABLED=1` به‌صورت پیش‌فرض فعال است.
با `V5_BRAIN_ENABLED=0` می‌توان رفتار قدیمی V4 را برای مقایسه اجرا کرد.

## اصل ایمنی

V5 حق شکستن سقف سخت ۱.۲۵٪ ریسک معامله را ندارد و سقف گرمای کل سبد نیز قبل از ورود بررسی می‌شود.
