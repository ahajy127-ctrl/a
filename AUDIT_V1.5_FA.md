# گزارش V1.5

## اصلاحات اصلی
- ریسک پایه معامله از ۸٪ به ۱٪ پیش‌فرض کاهش یافت و از `RISK_PER_TRADE` قابل تنظیم است.
- حد کِلی دیگر حداقل مصنوعی ۵٪ ندارد؛ بین ریسک پایه و سقف ۵٪ محدود می‌شود.
- تأیید صریح `LIVE_CONFIRM=I_UNDERSTAND_LIVE_TRADING` هم در راه‌اندازی و هم در مسیر ورود Live اجباری شد؛ اجرای مستقیم فایل پایتون نمی‌تواند این قفل را دور بزند.
- MACD به محاسبه واقعی خط MACD، خط سیگنال و هیستوگرام اصلاح شد.
- ADX از شمارش ساده حرکت‌ها به محاسبه مبتنی بر TR و +DI/-DI نزدیک به روش Wilder ارتقا یافت.
- ATR دیگر از قیمت لحظه‌ای به‌عنوان ATR جعلی استفاده نمی‌کند و از کندل‌های OHLC محاسبه می‌شود.
- شکستگی تاریخی بک‌تست در محاسبه ATR ثابت اصلاح شد تا از داده آینده برای تعیین Stop/TP استفاده نشود.
- مسیر سیگنال بک‌تست و Live از یک هسته امتیازدهی مشترک استفاده می‌کند؛ TITAN فقط نقش مشاور/تحقیقاتی دارد و سیگنال اجرایی را مخفیانه عوض نمی‌کند.
- منطق Breakout در TITAN اصلاح شد؛ سقف/کف با حذف کندل فعلی محاسبه می‌شود، بنابراین شرط شکست غیرممکن نیست.
- دوره پیش‌فرض Walk-Forward از ۲۱/۷ به ۹۰/۳۰ روز افزایش یافت.
- تست داخلی: 25/25 PASS.
- تست risk_guard: PASS.
- بسته Release بدون __pycache__ و فایل‌های runtime ساخته شد.

## محدودیت باقی‌مانده
این نسخه هنوز سوددهی را ثابت نمی‌کند. مرحله بعد باید داده تاریخی واقعی Hyperliquid Perpetuals را وارد یک موتور بازپخش کند که کندل، حجم، کارمزد، لغزش، Funding، Open Interest و منطق خروج را تا حد امکان همسان با Live شبیه‌سازی کند.
