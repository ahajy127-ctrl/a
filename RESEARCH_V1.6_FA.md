# آزمایشگاه تحقیق و بک‌تست V1.6

## چه چیزهایی اضافه شد؟

### ۱) ساعت واقعی بازارهای جهانی
به‌جای ساعت ثابت، از منطقه زمانی واقعی لندن، نیویورک و توکیو استفاده می‌شود؛ بنابراین تغییر ساعت تابستانی خودکار لحاظ می‌شود. ساعت بازار فقط یک عامل کیفیت است و به‌تنهایی دستور خرید یا فروش نیست.

### ۲) امتیاز کیفیت معامله
سیستم شواهد را به چند گروه مستقل تقسیم می‌کند تا مثلاً RSI و مومنتوم به‌عنوان دو تأیید کاملاً مستقل شمرده نشوند:
- روند و تایم‌فریم بالاتر
- مومنتوم
- نقدشوندگی و حجم
- بازار مشتقه
- جلسه بازار

خروجی ۰ تا ۱۰۰ است.

### ۳) ریسک تطبیقی
کیفیت بالاتر می‌تواند ریسک حساب را کمی افزایش دهد، اما سقف سخت ۱.۲۵٪ در هر معامله باقی می‌ماند. در ساعات کم‌حجم و شرایط طوفانی ریسک کاهش پیدا می‌کند.

### ۴) جداسازی ریسک، مارجین و اهرم
مارجین دیگر به‌صورت ساده از «درصد سرمایه × اهرم» تعیین نمی‌شود. ابتدا مقدار دلاری ریسک مشخص می‌شود، سپس با توجه به فاصله Stop و اهرم، مارجین لازم محاسبه می‌شود. بنابراین اهرم نباید ناخواسته ریسک معامله را چند برابر کند.

### ۵) بک‌تست درون‌ساعتی
بک‌تست جدید OHLC واقعی را می‌گیرد و در هر کندل بررسی می‌کند Stop یا Take Profit کدام زودتر لمس شده است. اگر هر دو در یک کندل لمس شوند، حالت محافظه‌کارانه Stop اول فرض می‌شود.

همچنین:
- کارمزد ورود و خروج
- لغزش قیمت
- Funding اختیاری
- اندازه موقعیت بر اساس ریسک
- جلسه بازار
- ریسک تطبیقی
- بدون استفاده از داده آینده

در محاسبه لحاظ می‌شوند.

### ۶) Walk-Forward
پیش‌فرض تحقیق:
- ۹۰ روز آموزش
- ۳۰ روز آزمایش خارج از نمونه

پنجره به‌صورت متحرک جلو می‌رود و نتیجه هر بخش آزمایش جداگانه ثبت می‌شود.

### ۷) Monte Carlo
ترتیب معاملات تصادفی می‌شود تا ببینیم افت سرمایه و نتیجه نهایی چقدر به ترتیب خاص معاملات وابسته است.

## اجرای آزمایشگاه

```bash
python backtest_lab.py --coins BTC,ETH,SOL --days 180 --walk-forward
```

خروجی در `research_data/` ذخیره می‌شود.

## داده

کندل‌ها مستقیماً از API عمومی Hyperliquid دریافت می‌شوند و به CSV ذخیره می‌شوند تا آزمایش قابل تکرار باشد. برای Funding نیز در صورت فعال بودن، تاریخچه Funding جمع‌آوری و به کندل‌ها متصل می‌شود.

## قانون مهم

تا زمانی که نتیجه خارج از نمونه، Walk-Forward و Monte Carlo مناسب نباشد، افزایش ریسک یا ورود با پول واقعی مجاز نیست.
